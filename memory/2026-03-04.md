# 2026-03-04

## 关键变更
- 排行榜表格新增并展示字段：`primaryTool`、`totalTokens`、`inputTokens`、`outputTokens`、`lastUpdated`。
- `build_snapshot.py` 在 ranking row 中输出上述字段，前端改为按新列渲染。
- mock 截图脚本同步输出上述字段，避免截图页面显示空值。

## UI 增量
- 页面新增 metric note，降低统计口径误解。
- 表格更新时间改为 relative time + title 显示绝对时间。
- 趋势图图例支持线条显隐，支持恢复显示。
