# 2026-03-03

## 关键决策
- 趋势图数据使用 snapshot 预计算，不在前端实时扫描 submissions。
- 前 5 用户跟随当前 Tool + Ranking 榜单。
- 时间窗口支持 7d / 14d / 30d / 90d。
- 图表采用原生 SVG，避免引入第三方图表依赖。

## 里程碑
- 已在 snapshot 新增 `timeseries` 字段。
- 已完成趋势逻辑与页面渲染接入。
- 已补充 Python + Node 测试并通过。

## 风险与结论
- 目前首版无 tooltip，后续如需精细交互再扩展。

## 增量记录
- 增加趋势图 hover tooltip（按悬浮日期显示值）。
- 增加截图回归校验脚本：命名规则、before/after 配对、PNG 尺寸一致性。

## Mock 截图决策
- 使用 `scripts/capture_mock_screenshots.py` 自动生成 mock 数据并截图。
- mock 数据落地到 `.local/mock-data`，截图落地到 `screenshots/YYYYMMDD/leaderboard/`。
- `.local/` 与 `screenshots/` 均加入 `.gitignore`，不纳入 git 追踪。
